apiVersion: v1
kind: ServiceAccount
metadata:
  name: payments-api
  namespace: payments-ref
  annotations:
    # Workload Identity binding placeholder — no real GCP SA email.
    iam.gke.io/gcp-service-account: payments-api@example-project-not-real.iam.gserviceaccount.com
